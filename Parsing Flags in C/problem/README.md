# Parsing Flags in C

**Problem Statement:**
Write a C program that handles three command-line options (`-x`, `-y`, `-z`). The `-x` option requires an associated string value, while `-y` and `-z` are simple toggles. Extract these arguments and print their final states to the console.
